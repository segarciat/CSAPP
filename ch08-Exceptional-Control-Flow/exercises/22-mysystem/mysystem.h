/*
 * Executes the named command.
 * If the command exits normally, returns its exit status.
 * Otherwise, if it terminates abnormally, returns the shell's exit status.
 */
int mysystem(char *command);
