/* Suspends the process for the specified number of seconds, then displays a messge
 * of how long it slept for.
 *
 * Returns 0 if slept the full amount, or the remaining seconds needed to finish.
 */
unsigned int snooze(unsigned int secs);
